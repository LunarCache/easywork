node_modules/
dist/
out/
build/
.turbo/
coverage/
*.tsbuildinfo

# Rust / Tauri build output
target/
apps/desktop/src-tauri/gen/

# Electron / packaging
release/
*.app
*.dmg
*.exe
*.AppImage

# Local data / models / secrets
*.local
.env
.env.*
!.env.example
# 仅忽略 repo 根的模型数据目录（早前 `models/` 误伤了源码 packages/core/src/models/，致其漏入库）。
/models/
*.gguf

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log
.playwright-mcp/
.superpowers/

# Node SEA 单文件 daemon 构建产物（本地/CI 生成，不入库）
apps/daemon/dist-sea/
